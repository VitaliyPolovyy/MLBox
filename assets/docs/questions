===== MLBoxWhat's scenario training of model with vast.ai
Now, I manually create folders, copy dataset, training script, install libs etc
It seems silly

===== RAY SERVE
- "MLBox\assets\configs\ray_serv_config.yaml": is location right?
- "scripts\main_ray_serve_.py": is location right?
- "main_ray_serve_.py": let's review code


===== ROOT DIR: "MLBox\mlbox\settings.py"
Is it right place to keep ROOT_DIR?


===== where should I keep constants which are related to specific service?
.env in ROOT_DIR ?
.env in service folder?
.config.py in service folder?
.config.py + .env in service folder ?

PEANUT_HF_REPO_ID=PolovyyVitaliy/peanuts-seg-yolo8s
PEANUT_HF_MODEL_FILE=peanuts-seg-yolo8s.pt
PEANUT_IMAGE_SIZE=1024
PEANUT_CROPPED_IMAGE_SIZE=100


===== What is the best practice for handling errors?
try - except?


===== image size for training, and for prediction, preprocessing

===== Batch processing, concurrency
